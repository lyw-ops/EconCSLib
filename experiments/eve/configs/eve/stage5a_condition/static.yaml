# @package _global_

stage5a_condition:
  id: static-no-specialized-guidance
  initial_guidance_mode: empty-runtime-directory
  optimizer_evolution: false
  retain_changed_guidance: false

loop:
  produce_optimizer_in_phase2: 0
