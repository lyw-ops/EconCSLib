# Boundary violation

The candidate changed a path outside the exact single-file edit contract.
Stop the Lean task, restore every forbidden workspace path, and rerun the
boundary command. A remaining violation receives `score: 0.0`.
