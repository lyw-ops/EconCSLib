# Entry Game transport route

Complete every declaration required by the seed README. Work only in
`Candidate.lean`, preserving the fixed exercise and abstract theorem prefix.
Construct the complete `RefinementCertificate`, use the fixed general
`AbstractTwoStage` theorems, and transport both conclusions back to concrete
profiles. A direct-only proof is not this route.

Read any supplied `guidance/`, use local Lean checks, and avoid placeholders,
new trusted declarations, native shortcuts, warning suppression,
evaluator/score material, network access, or out-of-bound edits. You may
improve `guidance/` with a concise reusable hint for a later iteration, but
the exact candidate declarations are the priority.
