# Entry Game direct route

Complete the three declarations required by the seed README. Work only in
`Candidate.lean`, preserve the fixed prefix and imports, and prove the claims
directly from the concrete payoff table. Do not introduce the abstract
transport template. Read any supplied `guidance/`, use local Lean checks, and
stop only when the exact declarations compile without default warnings.

Do not use placeholders, new trusted declarations, native shortcuts, warning
suppression, evaluator/score material, network access, or out-of-bound edits.
You may improve files under `guidance/` if a concise reusable proof hint would
help a later iteration, but the candidate proof is the priority.
