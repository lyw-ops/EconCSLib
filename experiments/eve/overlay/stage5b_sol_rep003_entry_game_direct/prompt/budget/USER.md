This rollout has {rollout_max_turns} turns. Use the exact
`/usr/bin/python3 STAGE5A_LEAN_CHECK.py` command after every candidate edit and
at least once before ending. Capture a reusable repair principle only after a
real recorded failure, and finish the exact declarations before the budget
ends.
