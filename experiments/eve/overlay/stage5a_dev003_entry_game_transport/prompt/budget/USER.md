This rollout has {rollout_max_turns} turns. Follow the ten transport
checkpoints, use the exact `/usr/bin/python3 STAGE5A_LEAN_CHECK.py` command
after every candidate edit and at least once before ending, and finish the
exact concrete declarations before the budget ends.
