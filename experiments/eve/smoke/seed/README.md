# Public synthetic EvE smoke seed

This directory is copied into an EvE solver workspace. It is a plumbing test
derived from the public `rep_dollar_syntax` fixture, not a formal benchmark
case. Only `Candidate.lean` is editable. No reference repair, evaluator, gold,
private data, or score implementation is present here.

