/goal 重新核验已冻结的 Stage 5B Sol REP-003、保持严格零模型边界，并停在等待单独明确执行授权的状态；本提示词绝不授权模型矩阵执行。

打开 /Users/lyuyuwei/Documents/EconCSlib，继续使用分支 experiment/eve-stage5a。

这是下一会话的完整任务提示词。先核对 live 事实，不要假定交接仍然最新。除非用户在另一条明确指令中授权 REP-003 模型/quota 消耗，否则不得执行、预约或创建正式状态。

一、先读并遵守

完整阅读根目录 AGENTS.md 和 experiments/eve/AGENTS.md，然后按顺序阅读：

- experiments/eve/HANDOFF.md
- experiments/eve/README.md
- experiments/eve/READINESS.md
- experiments/eve/stage5b_sol_rep002_protocol.json
- experiments/eve/stage5b_review/sol-rep002-execution-audit.json
- experiments/eve/stage5b_review/sol-rep002-post-archive-readonly-review.json
- experiments/eve/stage5b_sol_rep003_protocol.json
- experiments/eve/stage5b_sol_rep003_protocol.sha256
- experiments/eve/stage5b_sol_rep003_upstream_semantics.json
- experiments/eve/stage5b_review/sol-rep003-audit.json

检查：

- git status --short --branch
- git log 的最近提交
- 所有 remote 和 upstream tracking
- 本地 HEAD、upstream HEAD、规范远端分支 HEAD
- 外部 EvE checkout 和 clean Lean checkout 的精确 commit/clean 状态
- REP-003 正式 run-root parent 和 attempt ledger 仍不存在

规范仓库是 https://github.com/lyw-ops/EconCSLib，分支必须跟踪 fork/experiment/eve-stage5a。不要依赖提示词中的历史提交值；必须核对 live HEAD、upstream 和远端。如果状态已经前进，先阅读新增提交并重新确认任务边界。

工作树中 experiments/eve/ 之外可能存在大量用户自有修改。不得恢复、格式化、暂存、提交或吸收它们。没有必要时不要产生 tracked 修改；如必须修正文档或验证缺口，只处理本任务拥有的 experiments/eve/ 文件并逐项审阅 staged diff。

二、不可变历史

1. DEV-002、DEV-003、Sol REP-001 和 Sol REP-002 都已经消耗各自冻结的 attempt。不得 retry、resume、import、repair in place、删除、改写、checkpoint、清理或复用其 runtime state。

2. Sol REP-001 消耗 12 个 attempt 和 36 个 Sol session。Ordinal 12 因模型在最后一次 checker 后修改 Candidate.lean 而缺失完整 rollout telemetry。REP-001 不是 clean whole-matrix replication。

3. Sol REP-002 protocol v1.0.0 的 SHA-256 是：

318557e77f6b2126b813e522eea15bce03cb792639b2f537e4d53893749f7a8f

它保留 wrapper-owned post-agent/pre-evaluation terminal checker。12 个 attempt 已全部消耗；ordinal 1 在 reservation 后、模型前因外部 checkout hook 写权限失败而没有科学观测，绝不能重跑。Ordinals 2--12 完成 33 个 Sol session，33 条 completed/terminal event 一一对应，11 份 machine audit 可重放。REP-002 仍不是 clean whole-matrix replication。

4. REP-002 执行审计：

experiments/eve/stage5b_review/sol-rep002-execution-audit.json
SHA-256 f20db7bad909890a55ab40930f6464c59d149ae04abc6224ace5364893013709

第二遍 Codex AI 只读复核：

experiments/eve/stage5b_review/sol-rep002-post-archive-readonly-review.json
SHA-256 5b826fdfc17244076fe2aa9fcb49f5d1f2a778c6e03cf3b248da7258dbdbd918

两者均不是独立人工审查。

5. 外部 EvE 必须保持 pinned commit：

50b2399258ab08b6225a87cd05bded9701caa23d

clean Lean source 必须保持 commit：

b490317186ef435670c2eeb16050a214cdbf9fe5

混合 operator worktree 绝不能作为 Lean source。

6. experiments/eve/.runtime/ 被 Git 忽略，只存在本机。不得清理、修复或改写任何历史 root、ledger、transcript、token、lineage 或 audit evidence。

三、REP-003 冻结事实

Protocol identity：

EVE-STAGE5B-SOL-ENTRY-GAME-GUIDANCE-LIVENESS-REP-003

Version：1.0.0

SHA-256：

0acee4868aa626c21dfd27807423871734bd7437adcc292d5a964affd9db7505

状态：FROZEN_NOT_YET_EXECUTED / ZERO_MODEL_VALIDATED

REP-003 保留 REP-002 的所有科学输入和 terminal/rejection contract。它只新增一个启动修复：在 formal root、reserve_attempt 和任何模型访问之前，针对精确外部 EvE checkout 调用 pinned 的真实 write_repo_codex_hooks 两次，验证字节幂等、project trust 和全部 required hook-event trust。任何失败都必须在预约前 fail closed。

冻结时验证事实：

- 23 项 REP-003 targeted tests 通过；
- 完整 EVE suite 共运行 145 项：143 项通过，2 项 checkout-dependent 测试明确跳过；
- Stage 4、DEV-002、DEV-003、REP-001、REP-002、REP-003 verifier 全部通过；
- 12/12 REP-003 --check 通过真实 hook guard 与 exact-runtime preflight；
- 12/12 REP-003 --dry-run 通过，且不 dispatch guard 或 execute；
- model calls = 0；Sol quota consumed = 0；
- formal REP-003 root writes = 0；attempt-ledger writes = 0；
- historical state projection changes = 0；
- REP-003 run-root parent 和 attempt ledger 不存在；
- independent_human_review = false。

四、本提示词授权范围

允许：

- 只读核对 Git、协议、审阅记录、源码顺序、detached hash、frozen assets 和两个固定 checkout；
- 运行不会调用模型的 JSON/hash/verifier/test 检查；
- 在确有必要时运行 --dry-run；
- 只有在确有必要重新验证且明确确认其零模型语义后，才运行 --check；注意 --check 会对精确外部 EvE checkout 执行真实但幂等的 hooks.json 写入；
- 如发现 tracked 事实错误，在严格 EVE 范围内修正、验证、提交、推送并核对远端。

不允许：

- 任何旧或新的 --execute；
- 任何 Codex/ChatGPT/Luna/Sol 模型调用；
- 任何 subscription quota 或 API quota 消耗；
- --acknowledge-model-quota；
- 创建 REP-003 正式 run root、attempt ledger row、solver workspace、token record、telemetry 或 lineage database；
- retry/resume/import REP-001、REP-002 或任何历史状态；
- 创建或使用 OpenAI API key；
- 把本提示词解释为 REP-003 执行授权。

如果任何命令可能调用模型，立即停止。历史执行授权不转移到 REP-003。

五、当前会话的完成条件

1. 核对 live branch、remote hash、协议 hash、两个固定 checkout 和 REP-003 正式状态不存在性。
2. 核对 launcher 源码顺序确实是：真实 external-hook guard；其余 frozen source/dependency/auth/build/preflight gates；formal root 与 reserve_attempt；最后才是 upstream/model launch。
3. 核对 pre-execution review 的 claim boundary 明确排除 REP-003 outcome、完整 replication、完整 Sol-versus-Luna comparison、causal EvE effect、model capability、benchmark readiness、evaluation completion 和 independent human review。
4. 若无偏差，不要为制造提交而修改文件。报告已验证状态并停止。
5. 若有偏差，只在零模型边界内修复并完整验证；仅暂存任务拥有的 EVE 文件，提交、推送 fork/experiment/eve-stage5a，并用远端精确 hash 核对。
6. 最终回复必须明确写明：REP-003 未执行；模型/quota 未授权和未消耗；正在等待用户另行明确授权。

本提示词到此为止。不得运行 REP-003 模型矩阵。
