# PAIR pilot prompt

This directory contains one public Mathlib Style Pilot prompt and its compilable Lean context.
Private answer keys, provenance, validation, annotations, and ordering metadata are held separately.
